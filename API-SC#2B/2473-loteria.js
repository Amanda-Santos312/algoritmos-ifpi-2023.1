import {obter_texto, mostrar_texto} from '../modulo5-Strings/input-output.js'

function main() {
    const numeros_escolhidos = obter_texto().split(' ').map(Number)

    mostrar_texto(numeros_escolhidos)

    let numeros_sorteados = []

    

    //const iguais = verifica_numeros_iguais(numeros)
    
    
}

main()