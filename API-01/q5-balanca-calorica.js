import {question} from 'readline-sync'

function main() {
    const nome = question('Nome: ')
    const sexo = question('F ou M: ')
    const idade = Number(question('Idade: '))
    const peso = Number(question('Peso: '))
    const altura = Number(question('Altura: '))
    const ativ_fisica = Number(question('Nivel de Atividade Física de 1 a 1.48'))


}



main()